const { generateText } = require('ai'); const { groq } = require('@ai-sdk/groq'); require('dotenv').config(); async function main() { const {text} = await generateText({ model: groq('llama-3.1-8b-instant'), system: 'You are an intent analyzer. Output ONLY a JSON array of tool calls. If the user tells you to remember a fact or provides company knowledge, output [{\" "tool\: \learnFact\, \args\: {\fact\: \extracted" "fact\}}]. If no tools are needed, output [].', prompt: 'Teach the AI everything about ehfaaz. i also taught about positions about ehfaaz. but it didnt save anything' }); console.log(text); } main().catch(console.error);  
