const { generateText } = require('ai'); const { groq } = require('@ai-sdk/groq'); require('dotenv').config(); async function main() { const {text} = await generateText({ model: groq('llama-3.1-8b-instant'), system: 'You are an intent analyzer. Output ONLY a JSON array of tool calls. Available tools: [{\" "tool\: \learnFact\, \args\: {\fact\: \the" "fact\}}]. If the user tells you to remember/learn a fact, or tells you facts about their company (Ehfaaz), use learnFact. If no tools are needed, output [].', prompt: 'Aliyu is the CTO and Katherine is the CEO' }); console.log(text); } main().catch(console.error);  
