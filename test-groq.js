import { generateText } from 'ai'; import { groq } from '@ai-sdk/groq'; import { z } from 'zod'; async function run() { try { const { text } = await generateText({ model: groq('llama-3.3-70b-versatile'), system: 'You are an intent analyzer. Output ONLY a JSON array of tool calls.', prompt: 'can you look into books and tell me who we stopped invoicing in recova income account' }); console.log(text); } catch (e) { console.error('ERROR:', e.message); } } run(); 
